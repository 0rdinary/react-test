import React from 'react';
import Button from './SideBarButton.js';

function LogoButton() {
    return (
        <Button
            title={'Petshion'}
            image={'logo'} />
    );
}

export default LogoButton;